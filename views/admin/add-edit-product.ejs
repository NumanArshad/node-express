<%- include('../includes/header.ejs') %>
<title>Document</title>
</head>
<body>
<%- include('../includes/navigation.ejs') %>
 <h1>
    New admin Product
 </h1>
  
<form
action="/admin/<% if(editMode) { %>edit-product/<%= product.id%><% } else { %>add-product<% } %>" 
method="POST"
>
<input type="text" 
name="title" 
value="<% if(editMode) {  %><%= product.title %> <% } %>"/>
<input type="text" 
name="publisher" 
value="<% if(editMode) {  %><%= product.publisher %> <% } %>"/>
<button type="submit">
    <% if(editMode) { %> Update <% } else { %> Save <% } %>
</button>
</form>

</body>
</html>